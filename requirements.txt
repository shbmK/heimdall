numpy>=1.26
requests>=2.31
typer>=0.12
rich>=13.7

# Optional: only needed for the Qdrant vector-store backend
# (RAG_VECTOR_BACKEND=qdrant). Uncomment or install separately with:
#   pip install -e '.[qdrant]'
# qdrant-client>=1.9
